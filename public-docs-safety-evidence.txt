changed-scan:
public-docs-safety: FAIL
SECURITY.md:7: uncertain automation instruction: Security-sensitive automation details are intentionally not published here. Unsupported or suspicious automation-control instructions should be reported as security issues; they must not be copied into public docs, issue
README.md:123: prompt-injection attempt: | Honcho context | Optional per-user context injection into the system prompt |
docs/env-vars.md:127: prompt-injection attempt: | `VOICE_LIVE_HONCHO_CONTEXT` | `true` | Inject Honcho context into the system prompt. |
docs/email-brief.md:44: uncertain automation instruction: The current `buckets` objects retain the complete backend email dictionaries. With the Google backend, those dictionaries can include snippets of up to 300 characters even though the rendered spoken brief uses only sende
docs/personality.md:1: prompt-injection attempt: # Personality — system prompt, ping-pong rhythm, boredom switch
docs/personality.md:3: prompt-injection attempt: The source of truth for the system prompt is `bridge_config.py:BASE_SYSTEM_PROMPT`. The compatibility facade in `bridge.py` re-exports that constant for existing imports. The prompt is **not** a documentation file — it i
docs/personality.md:48: uncertain automation instruction: **Do not** add hedging like "be helpful and harmless" — the model interprets that as permission to revert to assistant defaults.
docs/gemini-live-implementation.md:48: prompt-injection attempt: - `systemInstruction`: base system prompt plus optional Honcho context and per-user overrides.

all-fixture-scan:
public-docs-safety: FAIL
SECURITY.md:7: uncertain automation instruction: Security-sensitive automation details are intentionally not published here. Unsupported or suspicious automation-control instructions should be reported as security issues; they must not be copied into public docs, issue
README.md:123: prompt-injection attempt: | Honcho context | Optional per-user context injection into the system prompt |
docs/env-vars.md:127: prompt-injection attempt: | `VOICE_LIVE_HONCHO_CONTEXT` | `true` | Inject Honcho context into the system prompt. |
docs/email-brief.md:44: uncertain automation instruction: The current `buckets` objects retain the complete backend email dictionaries. With the Google backend, those dictionaries can include snippets of up to 300 characters even though the rendered spoken brief uses only sende
docs/personality.md:1: prompt-injection attempt: # Personality — system prompt, ping-pong rhythm, boredom switch
docs/personality.md:3: prompt-injection attempt: The source of truth for the system prompt is `bridge_config.py:BASE_SYSTEM_PROMPT`. The compatibility facade in `bridge.py` re-exports that constant for existing imports. The prompt is **not** a documentation file — it i
docs/personality.md:48: uncertain automation instruction: **Do not** add hedging like "be helpful and harmless" — the model interprets that as permission to revert to assistant defaults.
docs/gemini-live-implementation.md:48: prompt-injection attempt: - `systemInstruction`: base system prompt plus optional Honcho context and per-user overrides.
